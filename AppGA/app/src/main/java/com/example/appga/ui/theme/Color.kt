package com.example.appga.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales modernos
val Primary = Color(0xFF2563EB) // Azul moderno
val PrimaryVariant = Color(0xFF1D4ED8)
val Secondary = Color(0xFF10B981) // Verde esmeralda
val SecondaryVariant = Color(0xFF059669)

// Colores de superficie
val Surface = Color(0xFFF8FAFC)
val SurfaceVariant = Color(0xFFE2E8F0)
val Background = Color(0xFFFFFFFF)

// Colores de acento
val Accent = Color(0xFFEF4444) // Rojo para errores/eliminar
val AccentSecondary = Color(0xFFF59E0B) // Amarillo para advertencias
val Success = Color(0xFF10B981) // Verde para éxito

// Colores de texto
val OnPrimary = Color(0xFFFFFFFF)
val OnSecondary = Color(0xFFFFFFFF)
val OnSurface = Color(0xFF1E293B)
val OnBackground = Color(0xFF0F172A)

// Colores para modo oscuro
val DarkPrimary = Color(0xFF3B82F6)
val DarkSurface = Color(0xFF1E293B)
val DarkBackground = Color(0xFF0F172A)
val DarkOnSurface = Color(0xFFE2E8F0)
